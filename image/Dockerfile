ARG PYTHON_V=3.7
ARG AIRFLOW_V=2.1.2

FROM python:$PYTHON_V

ENV AIRFLOW_HOME=/
ENV AIRFLOW__CORE__LOAD_EXAMPLES=False
ENV AIRFLOW_VERSION=${AIRFLOW_V}

WORKDIR ${AIRFLOW_HOME}

COPY ./script/ .
RUN . ./setup.sh

CMD ["python", "start.py"]
